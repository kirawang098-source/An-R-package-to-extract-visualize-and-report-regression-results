# install devtools if needed
if (!requireNamespace("devtools", quietly = TRUE)) install.packages("devtools")

# install regresult from GitHub
devtools::install_github("kirawang098-source/An-R-package-to-extract-visualize-and-report-regression-results")
